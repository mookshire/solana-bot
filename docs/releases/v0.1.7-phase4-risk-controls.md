# v0.1.7 — Phase 4: Risk Controls

**New**
- `src/risk_controls.py`: ATR (Wilder) + `calc_stops()` + `position_size_units()`.
- `src/backtest_with_risk.py`: EMA(20/55) strategy with ATR SL/TP and ATR-based position sizing.
- `configs/phase4_balanced.env`: recommended preset (SL=1.5×ATR, TP=4.0×ATR, risk=0.5%).
- `src/phase4_sweep.py`: grid sweep over SL/TP/risk → `reports/phase4_sweep_results.csv`, `phase4_sweep_top10.md`.
- `src/phase4_compare.py`: baseline vs risk comparison → `reports/phase4_compare_baseline_vs_risk.{md,csv}`.
- `scripts/run_backtest.sh`: main runner (`USE_RISK=1|0`).

**Artifacts**
- `reports/phase4_with_risk_summary*.json`, `phase4_trades*.csv`, `phase4_equity_curve*.csv`, `phase4_monthly_risk*.csv`.
- Tag history: v0.1.3-phase4-start → v0.1.4-phase4-balanced → v0.1.5-phase4-main-run → v0.1.6-phase4-compare.

**Notes**
- Data comes from existing `backtest_hybrid.fetch_klines()` when available; otherwise uses cached JSON or synthetic fallback.
- All scripts auto-commit successful runs via `scripts/save.sh`.
